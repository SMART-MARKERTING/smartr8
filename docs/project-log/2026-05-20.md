# Project log — 2026-05-20

Audit trail of the smartr8 work in this session. Three PRs merged to
production (#6, #7, #8); one in review (#9).

---

## PR #6 — Mobile, consent, v2 pages, performance (merged, live)

- [x] **Mobile fixes:** removed `maximum-scale=1` (restored pinch-zoom);
  scaled down Home hero H1 + headshot; stacked HelocInstantOptions trust
  strip on phones; scaled Thank You H1 + Cal.com embed height
- [x] **Consent checkbox made optional on all 6 lead forms** (HelocQuick,
  HelocForm, Heloc, Worksheet, LeadCaptureModal, ExportLeadModal) —
  submission is the consent act (sign-in-wrap); each lead now records
  `consent_box_checked`
- [x] **Built `/heloc/quick-v2`** (HelocQuickV2) — compact hero, trust
  pills above form, mobile sticky CTA
- [x] **Built `/heloc/instant-options-v2`** (HelocInstantOptionsV2) —
  rewritten option cards, no em dashes, `OptionSelected` custom event
- [x] **`funnel_version: "v2"`** tagging on all v2 pixel events
- [x] **Performance pass:** optimized logos (eho 25 to 4.2 KB, adaxa 7.3
  to 5.6 KB); self-hosted Inter variable woff2 + preload;
  `build.target: es2020`; `public/_headers` cache rules; analytics moved
  to end of `<body>`; eager-loaded v2 routes; non-blocking CSS
  (preload + onload swap); removed static canonical
- [x] **`legacyAssetWarn`** runtime console warning for stale logo paths
- [x] **CHANGELOG.md** created

## PR #7 — Worksheet (merged, live)

- [x] **Custom APR override** on `/worksheet/internal` (type your Loan
  Estimate APR; blank = auto-calc from closing costs)
- [x] **Savings strategy reframed** from "automatically redirected" to
  conditional "by applying ... if applied to principal" across copy and
  table labels (on-screen + PDF)

## PR #8 — Phone backend, whats-next, CI (merged, live)

- [x] **Phone made optional server-side** in `/api/submit-lead` (the real
  bug: the form said optional but the Worker rejected blank phones);
  dedup guarded against an empty phone key
- [x] **Removed "Run the Numbers / Loan Benefits Worksheet" card** from
  `/heloc/whats-next` (grid rebalanced 4 to 3 columns)
- [x] **Step-9 consent disclosure** clarified ("submits even if
  unchecked")
- [x] **`actions/checkout@v4` to `v5`** (clears the Node.js 20
  deprecation warning)

## PR #9 — `/heloc-v2` full funnel (OPEN, merge held for review)

- [x] Built `/heloc-v2` (HelocV2) — same 9 steps and fields,
  mobile-first, cream/green accents, full compliance Footer, warmer
  first-person copy, no dashes or jargon
- [x] Review fixes: disabled CTA renders gray (no salmon) and solid red
  when enabled; sticky-CTA overlap padding; tightened step-1 hero so both
  name labels show at first paint; whats-next name trim + fallback
- [x] Step-9 submit routes to `/heloc/instant-options-v2` with
  `name/credit/use/timeline` params (control `/heloc` still routes to
  `/heloc/whats-next`)
- [x] Personalized banner + profile-based emphasis badge on
  instant-options-v2 (credit 700+ and not self-employment-funded =>
  Fast Digital "Fastest Option"; otherwise Flexible "Recommended");
  quick path defaults to Flexible
- [x] Meta `Lead` fires on step-9 submit (`variant: "A"` +
  `funnel_version: "v2"`); GA4 `generate_lead` on submit
- [ ] Verification: PSI mobile + visual checklist (CTA color, sticky
  overlap, step-1 hero, redirect + banner, quick-path banner, Lead in
  Test Events)
- [ ] Conditional: if PSI LCP > 5.5s, ship the lazy route-chunk split
  into PR #9 (pre-authorized)
- [ ] Merge once approved

## Discussed, no code (owner action)

- [ ] **Set `VITE_GOOGLE_PLACES_API_KEY`** in Cloudflare Pages env
  (Production + Preview) and redeploy. Until then, address autocomplete
  falls back to a plain text field on every funnel. (Mykoal's task.)
- Answered: Google Maps Platform billing (free tier + quota-cap guidance)
  and the TCPA / consent-checkbox legal read

## Backlog parked in CHANGELOG (post-launch)

- [ ] `manualChunks` route-split so ad-traffic visitors do not carry the
  home/worksheet code
- [ ] Defer Meta Pixel + GTM behind idle (verify Lead still fires)
- [ ] Critical-CSS pre-render (only if SSR is ever adopted)
- [ ] Delete legacy `/eho-logo.png` + `/adaxa-logo.jpg` + the
  `legacyAssetWarn` module once confirmed unreferenced
